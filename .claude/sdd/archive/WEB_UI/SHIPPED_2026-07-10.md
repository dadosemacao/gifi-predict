# SHIPPED: Superfície de Uso Web (Camada 5 GIFI)

> Feature shipped on 2026-07-10

## Metadata

| Attribute | Value |
|-----------|-------|
| **Feature** | WEB_UI |
| **Ship Date** | 2026-07-10 |
| **Author** | Emerson Antônio (ship-agent) |

---

## Summary

Implementada a **Camada 5 GIFI** — superfície web local homologável para upload de cenários Modo A/B, validação online (L2), inferência cascata (L3) e consumo da política de release (L4). Stack: **FastAPI** (`src/serving/`) + **React/Vite** (`web/`). Opera em **modo demonstração** com banner visível enquanto `release_ok=false` (MAE atual ~96); bloqueia bind produtivo com HTTP 403 quando `demo=false`.

---

## Timeline

| Milestone | Date | Duration |
|-----------|------|----------|
| Define Complete | 2026-07-10 | — |
| Design Complete | 2026-07-10 | — |
| Build Complete | 2026-07-10 | — |
| **Shipped** | 2026-07-10 | **Total: 1 dia** |

---

## Metrics

| Metric | Value |
|--------|-------|
| **Files Created** | 48 (manifesto DESIGN) |
| **Lines of Code** | ~1.520 (`src/serving/`, `tests/serving/`, `web/src/`) |
| **Tests Written** | 12 (9 pytest + 3 vitest) |
| **Test Pass Rate** | 100% |
| **Package Version** | `gifi-predict` 0.3.0 |
| **New Dependencies** | `fastapi`, `uvicorn[standard]`, `python-multipart`; Node: React, Query, Recharts |

---

## What Was Built

### Components

| Component | Description |
|-----------|-------------|
| Serving API | `POST /api/scenario/validate`, `/api/simulate`, `GET /api/release-status`, `/api/template` |
| Release policy | Lê `acceptance_report.json`; 403 prod bloqueado; demo permitido |
| Champion loader | `load_production_champion_pipes` via `current_champion.json` |
| React SPA | Upload, curvas Recharts, top-3 detratores, badge Demo/Release |
| Dev tooling | `serve run`, `scripts/dev_ui.sh`, Vite proxy `/api` → :8000 |

### Key Paths

| Path | Purpose |
|------|---------|
| `src/serving/` | FastAPI Camada 5 |
| `web/` | SPA Vite + React |
| `config/serving.yaml` | `default_run_id`, host, paths |
| `tests/serving/` | AT-UI pytest |
| `web/src/features/` | scenario-upload, curves, detractors |

---

## Success Criteria Verification

| Criterion | Target | Actual | Status |
|-----------|--------|--------|--------|
| Upload Modo A/B + curvas | MUST | pytest + API 200 | ✅ |
| Banner demo quando `demo_mode=true` | MUST | Vitest AT-UI-005 | ✅ |
| 403 prod sem release | MUST | `test_prod_blocked_when_not_released` | ✅ |
| Top-3 detratores | MUST | simulate service + panel | ✅ |
| Regras fora do JSX | MUST | hooks/services/schemas | ✅ |
| p95 < 10s (500 linhas) | NFR | Não medido (@slow pending) | ⚠️ |
| Playwright E2E | COULD | Não implementado | — |

---

## Lessons Learned

### Process

- Design judge manual caught bug crítico (`load_champion_pipes` ≠ prod bind) antes do BUILD — validar APIs existentes contra decisões de gate.
- SDD com manifesto de 48 arquivos permitiu BUILD autônomo em uma sessão.

### Technical

- `IngestSettings.l2_path` e `SimulationSettings.l2_path` devem estar alinhados no simulate — publicar e ler `infer_features.parquet` no mesmo root.
- Detratores on-the-fly exigem merge de predições cascata antes do SHAP na linha-âncora.
- Vite proxy simplifica dev; FastAPI `StaticFiles` serve prod local com `web/dist`.

### Communication

- Open questions fechadas no DEFINE (layout `web/`, detratores on-the-fly, champion loader) evitaram retrabalho no BUILD.

### Tools & Libraries

- FastAPI + Pydantic v2 espelham DTO TypeScript com baixo atrito.
- TanStack Query + RHF/Zod mantém regra de negócio fora dos componentes presentacionais.

---

## Recommendations for Future Work

| Area | Recommendation |
|------|----------------|
| Performance | Adicionar `@slow` AT-UI-010/011 no CI |
| Release | Quando `release_ok=true`, validar AT-UI-007 end-to-end com champion real |
| UX | Seletor de `run_id` (COULD); export JSON snapshot |
| Ops | Cleanup L2 `ui-{uuid}` > 24h; Playwright homologação stakeholder |
| Modelagem | Gate verde MAE ≤ 56 desbloqueia bind produtivo na mesma UI |

---

## Archived Artifacts

| Artifact | Location |
|----------|----------|
| DEFINE | `./DEFINE_WEB_UI.md` |
| DESIGN | `./DESIGN_WEB_UI.md` |
| BUILD_REPORT | `./BUILD_REPORT_WEB_UI.md` |
| SHIPPED | `./SHIPPED_2026-07-10.md` (this file) |

---

## Acknowledgments

Camada 5 fecha o loop L1→L5 do MVP GIFI para homologação assistida Marco 2 (31/08/2026) em modo demo, sem mascarar `release_ok=false`.

---

*Feature archived on 2026-07-10 by ship-agent*
